services:
  processor:
    command: uvicorn app.app.server:app --host 0.0.0.0 --port 8000
    build:
      context: ./processor
    env_file:
      - .env
    ports:
      - "15000:5000"
      - "8001:8000"
    restart: always
