FROM python:3.11-slim

WORKDIR /app

# Copy ENTIRE project so imports work
COPY ../ ./

# Install requirements if you have them
RUN pip install --no-cache-dir fastapi uvicorn requests

# Start the crypto payout FastAPI service
CMD ["uvicorn", "crypto_app:app", "--host", "0.0.0.0", "--port", "9001"]
